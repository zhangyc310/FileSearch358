# FileSearch 使用指南

## 🚀 快速开始

### 1. 安装依赖

```bash
# 创建虚拟环境（推荐）
python -m venv venv

# 激活虚拟环境
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# 安装依赖
pip install -r requirements.txt
```

### 2. 运行应用

```bash
python main.py
```

## 📖 使用说明

### 索引文件

1. 点击 **"选择目录"** 按钮，选择要索引的文件夹
2. 点击 **"开始索引"** 按钮，等待索引完成
3. 进度条会显示索引进度

### 智能同步（重要功能）

**适用场景**: 程序停用一段时间后（如几天、一周），期间文件有变化

#### 自动检测（推荐）
- 程序启动时，如果检测到已有索引，会自动提示是否同步
- 点击"是"即可自动检测文件变化并更新索引

#### 手动同步
1. 选择之前索引过的目录
2. 点击 **"🔄 智能同步"** 按钮
3. 系统会自动：
   - 📄 检测新增的文件并索引
   - 🔄 检测修改的文件并更新
   - 🗑️ 删除已删除文件的索引
   - ✔️ 跳过未变化的文件

**优势**:
- 比完全重建索引快得多（只处理变化的文件）
- 适合文件数量很多但变化不大的场景
- 基于文件修改时间戳，准确可靠

### 搜索文件

1. 在搜索框中输入关键词
2. 选择搜索类型：
   - **内容搜索**: 在文档内容和文件名中搜索
   - **文件名搜索**: 仅在文件名中搜索
3. 点击 **"🔍 搜索"** 按钮或按 **Enter** 键
4. 双击搜索结果中的路径行可以打开文件

### 管理索引

- **清空索引**: 点击 **"清空索引"** 按钮可以删除所有索引数据
- **增量更新**: 索引完成后，文件的变化会自动更新到索引中（需要重新索引）

## 📁 支持的文件格式

### 内容索引（全文搜索）
- **PDF** (.pdf) - 使用 PyMuPDF 提取
- **Word 文档** (.docx) - 使用 python-docx 提取
- **旧版 Word** (.doc) - Windows 使用 pywin32，Linux 需要 antiword
- **Markdown** (.md) - 直接读取
- **文本文件** (.txt) - 直接读取，支持多种编码

### 文件名索引
- **所有文件类型**，包括：
  - 视频: mp4, avi, mkv, mov, wmv, flv, webm 等
  - 音频: mp3, wav, flac, aac, ogg, wma 等
  - 图片: jpg, png, gif, bmp 等
  - 其他任意文件类型

## ⚙️ 高级配置

### 性能优化

对于大量文件（几千个）：
- 索引过程可能需要几分钟
- Whoosh 索引采用增量写入，内存占用低
- 搜索响应通常在毫秒级

### Linux/Mac 上处理 DOC 文件

如果需要索引 .doc 文件，需要安装 antiword：

```bash
# Ubuntu/Debian
sudo apt-get install antiword

# macOS
brew install antiword
```

## 🔍 搜索技巧

### 基本搜索
```
关键词
```

### 多个关键词（AND）
```
关键词1 AND 关键词2
```

### 任意关键词（OR）
```
关键词1 OR 关键词2
```

### 排除关键词（NOT）
```
关键词1 NOT 关键词2
```

### 短语搜索
```
"完整短语"
```

### 通配符
```
关键词*     # 匹配 关键词、关键词语、关键词汇 等
```

## 🐛 常见问题

### Q: 索引后找不到文件？
A: 检查以下几点：
1. 确认文件格式是否支持内容索引
2. 对于不支持内容索引的文件，使用"文件名搜索"
3. PDF 文件可能是扫描版，无法提取文字

### Q: 中文搜索不准确？
A: Whoosh 对中文分词支持有限，建议：
1. 使用完整的词语搜索
2. 使用短语搜索（加引号）

### Q: 索引占用空间大？
A: 索引大小通常是文本内容的 10-20%，这是正常的

### Q: 如何打包成独立应用？
A: 使用 PyInstaller：

```bash
pip install pyinstaller
pyinstaller --onefile --windowed --name FileSearch main.py
```

## 📊 技术架构

```
┌─────────────────────────────────────┐
│     CustomTkinter UI (main_window)  │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│   Search Engine (Whoosh Index)      │
│   - 索引管理                         │
│   - 搜索查询                         │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│   Document Parser                    │
│   - PDF (PyMuPDF)                   │
│   - DOCX (python-docx)              │
│   - DOC (pywin32/antiword)          │
│   - TXT/MD (直接读取)                │
└─────────────────────────────────────┘
```

## 📝 许可证

MIT License - 可自由使用和修改
